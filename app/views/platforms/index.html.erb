<div class="container-fluid">
  <h1>Platforms</h1>

  <div id="platforms">
    <% @platforms.each do |p| %>
      <div class="row">
        <div class="col-md-2">
         <%= link_to "#{p.name}", platform_path(p) %>
        </div>
        <div class="col-md-10">
          <%= p.description %> 
        </div>
      </div>
    <% end %>
  </ul>

  <div class="row">
    <div class="col-md-12">
      <%= link_to "New Platform", new_platform_path %>
    </div>
  </div>
</div>